# Song

- Title: {{ audio_profile.song_identity.title or "Untitled" }}
- Artist: {{ audio_profile.song_identity.artist or "Unknown" }}
- Duration: {{ (audio_profile.song_identity.duration_ms / 1000) | round(1) }}s
- BPM: {{ audio_profile.song_identity.bpm }}
- Overall energy: {{ audio_profile.energy_profile.macro_energy.value }}

## Authoritative Song Structure

Create exactly {{ audio_profile.structure.sections | length }} typed section entries:
{% for section in audio_profile.structure.sections %}
- {{ section.section_id }}: {{ section.start_ms }}ms–{{ section.end_ms }}ms
{% endfor %}

## Theme Catalog

{% for theme in theme_catalog %}
- {{ theme.theme_id }} — {{ theme.title }}
{% endfor %}

## Palette Catalog

{% for palette in palette_catalog %}
- {{ palette.id }}{% if palette.title is defined %} — {{ palette.title }}{% endif %}
{% endfor %}

## Motif Catalog

{% for motif in motif_catalog %}
- {{ motif.id }}{% if motif.energy is defined %} ({{ motif.energy }}){% endif %}{% if motif.description is defined %} — {{ motif.description }}{% endif %}
{% endfor %}

## Display Targets

Group IDs:
{% for group in display_groups %}
- {{ group.id }}{% if group.element_kind is defined %} ({{ group.element_kind }}){% endif %}
{% endfor %}

{% if available_zone_ids is defined and available_zone_ids %}
Zone IDs: {{ available_zone_ids | join(', ') }}
{% endif %}
{% if available_split_ids is defined and available_split_ids %}
Split IDs: {{ available_split_ids | join(', ') }}
{% endif %}

Typed targets use the response schema's target discriminator and an exact supplied ID.

## Audio Guidance

- Recommended motion density: {{ audio_profile.creative_guidance.recommended_motion_density.value }}
- Recommended contrast: {{ audio_profile.creative_guidance.recommended_contrast.value }}
{% if audio_profile.creative_guidance.cautions %}
{% for caution in audio_profile.creative_guidance.cautions %}
- Caution: {{ caution }}
{% endfor %}
{% endif %}

{% if lyric_context is defined and lyric_context %}
## Lyric Guidance

- Themes: {{ lyric_context.themes | join(', ') }}
- Mood arc: {{ lyric_context.mood_arc }}
- Narrative: {{ "yes" if lyric_context.has_narrative else "no" }}
{% for beat in lyric_context.story_beats | default([]) %}
- {{ beat.section_id }}: {{ beat.description }}
{% endfor %}
{% endif %}

# Task

Create the complete typed MacroPlan. Make palette changes, motif recurrence, focal
rotation, and call-and-response intentional and mutually consistent. Keep `notes`
section-scoped and do not prescribe downstream effects or exact timing.
