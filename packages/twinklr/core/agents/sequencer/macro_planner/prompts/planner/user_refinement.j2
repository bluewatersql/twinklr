# Revise the Typed MacroPlan

Iteration {{ iteration + 1 }} feedback:

{{ feedback }}

{% if revision_request is defined and revision_request %}
Priority: {{ revision_request.priority }}
{% for fix in revision_request.specific_fixes | default([]) %}
- {{ fix }}
{% endfor %}
{% endif %}

Keep all valid work, but return the complete revised four-field MacroPlan. Preserve exact
audio section IDs/timestamps and repair references atomically across `sections`,
`palette_arc`, `motif_continuity`, and `focal_arc`. Use only supplied catalog and display
IDs. Recheck active palette stops, one matching LEAD per section, motif references, and
distinct call/response targets before responding.
