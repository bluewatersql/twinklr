# Evaluation Context

Song: {{ audio_profile.song_identity.title or "Untitled" }} by {{ audio_profile.song_identity.artist or "Unknown" }}

Expected sections:
{% for section in audio_profile.structure.sections %}
- {{ section.section_id }}: {{ section.start_ms }}ms–{{ section.end_ms }}ms
{% endfor %}

Valid group IDs:
{% for group in display_groups %}
- {{ group.id }}
{% endfor %}
{% if available_zone_ids is defined and available_zone_ids %}
Valid zones: {{ available_zone_ids | join(', ') }}
{% endif %}
{% if available_split_ids is defined and available_split_ids %}
Valid splits: {{ available_split_ids | join(', ') }}
{% endif %}

Valid palettes:
{% for palette in palette_catalog | default([]) %}
- {{ palette.id }}
{% endfor %}

Valid motifs:
{% for motif in motif_catalog | default([]) %}
- {{ motif.id }}
{% endfor %}

# Typed MacroPlan

Palette arc:
{% for stop in macro_plan.palette_arc %}
- {{ stop.stop_id }} from {{ stop.applies_from_section_id }}: {{ stop.palette.palette_id }} via {{ stop.transition.value }}
{% endfor %}

Motif continuity:
{% for thread in macro_plan.motif_continuity %}
- {{ thread.motif_id }} [{{ thread.evolution.value }}]: {{ thread.section_ids | join(', ') }} — {{ thread.description }}
{% endfor %}

Focal arc:
{% for assignment in macro_plan.focal_arc %}
- {{ assignment.section_id }}: {{ assignment.lead_target.type.value }}:{{ assignment.lead_target.id }}
{% endfor %}

Sections ({{ macro_plan.sections | length }}):
{% for section in macro_plan.sections %}
## {{ section.section.section_id }} ({{ section.section.start_ms }}ms–{{ section.section.end_ms }}ms)
- energy={{ section.energy_target.value }}, density={{ section.motion_density.value }}, style={{ section.choreography_style.value }}
- palette stop={{ section.palette_role.stop_id }}{% if section.palette_role.override %}, override={{ section.palette_role.override.palette_id }}{% endif %}
- theme={{ section.theme.theme_id }}, theme palette={{ section.theme.palette_id or "null" }}
- motifs={{ section.motif_ids | join(', ') }}
- focal roles:{% for focal in section.focal_roles %} {{ focal.role.value }}={{ focal.target.type.value }}:{{ focal.target.id }}{% endfor %}
- call/response:{% for pair in section.call_response_pairs %} {{ pair.call.type.value }}:{{ pair.call.id }} -> {{ pair.response.type.value }}:{{ pair.response.id }} ({{ pair.step_duration }} {{ pair.step_unit.value }}){% else %} none{% endfor %}
- coordination={{ section.coordination_intent.value }}
- notes={{ section.notes }}
{% endfor %}

Evaluate technical validity and strategic quality. Return the complete structured verdict.
