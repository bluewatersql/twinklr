"""Resolution logic for moving heads domain.

Consolidates all resolution logic in one place:
- context: Resolution context and MovementResolver interface
- registry: Resolver registry (for sequencer path)
- target_resolver: Fixture target resolution
- template_resolver: Template resolution
- template_handler_registry: Template handler registry (for template path)
"""
