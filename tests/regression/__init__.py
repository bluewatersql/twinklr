"""Post-refactor `.xsq` regression/parity harness.

Validates that the current deterministic pipeline still emits xLights sequences whose
technical sophistication is on par with pre-refactoring baselines captured under
`baselines/`. See `changes/post-refactor-validation/`.
"""
