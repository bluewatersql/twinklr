---
type: change
status: active
area:
created:
updated:
---

# <Change Name>

_Spec for `changes/<slug>/`. Delete sections that don't apply — see
[changes/INDEX.md](../changes/INDEX.md) for lifecycle and closure rules._

## Objective

What this change accomplishes and why.

## Scope

In scope / out of scope.

## Design

Approach, key decisions (link `memories/decisions/` records for the big ones), affected
subsystems (link relevant `context/` docs).

## Plan

Ordered implementation steps; note validation per step (`make validate`).

## Validation

Evidence the change works: test output, benchmarks, manual checks.
