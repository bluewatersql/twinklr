---
type: context
area:
updated:
---

# <Document Title>

Current project truth for one domain. Concise: summarize, then link into `docs/` and
source paths for depth — don't duplicate them. State what *is*, not what is planned
(planned/in-flight work belongs in `changes/`). Add the document to
[context/INDEX.md](../context/INDEX.md).
